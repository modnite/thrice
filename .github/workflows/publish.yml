name: Publish image

# Builds the THRICE image and pushes it to GitHub's container registry (ghcr.io), so the server only ever pulls.
#   - every push to main   -> ghcr.io/<owner>/thrice:main and :sha-<commit>
#   - a tag like v1.2.0    -> ghcr.io/<owner>/thrice:1.2.0, :1.2 and :latest
# Deploy a specific version with THRICE_TAG in the stack's .env.

on:
  push:
    branches: [main]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/thrice
          tags: |
            type=ref,event=branch
            type=sha,prefix=sha-
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable=${{ startsWith(github.ref, 'refs/tags/v') }}

      - uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          # linux/amd64 only: building arm64 under emulation makes the Next.js build very slow.
          platforms: linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max
